%p#notice= notice
%h1 Listing Services
%table
  %thead
    %tr
      %th Name
      %th Description
      %th Picture
      %th Price
      %th{:colspan => "3"}
  %tbody
    - @services.each do |service|
      %tr
        %td= service.name
        %td= service.price
        %td= link_to 'Show', service
        %td= link_to 'Edit', edit_admin_service_path(service)
        %td= link_to 'Destroy', admin_service_path(service), method: :delete
%br/
= link_to 'New Service', new_admin_service_path