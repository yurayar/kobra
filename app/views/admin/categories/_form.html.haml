= simple_form_for ([:admin, @category]) do |f|
  .form-inputs
    = f.label :name
    = f.text_field :name
    = f.label :article_id
    = f.number_field :article_id
  .form-actions
    = f.submit 'Save'
