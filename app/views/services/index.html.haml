%p#notice= notice
%h1 Listing Services
%table
  %thead
    %tr
      %th Name
      %th Description
      %th Picture
      %th Price
      %th{:colspan => "3"}
  %tbody
    - @services.each do |service|
      %tr
        %td= service.name
        %td= service.description
        %td= service.picture
        %td= service.price
        %td= link_to 'Show', service
%br/